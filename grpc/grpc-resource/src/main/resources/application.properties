spring.application.name=grpc-resource
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:9090
# 9090 is the authorization server (issuer-uri above), so the gRPC server gets its own port
spring.grpc.server.port=8086